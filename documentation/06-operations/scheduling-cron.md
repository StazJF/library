**Scheduled Tasks**
- No scheduled tasks are defined in the console kernel. Source: `app/Console/Kernel.php`.

**Cron Setup**
- If scheduling is added later, use Laravel's standard scheduler command: `php artisan schedule:run`. Source: `app/Console/Kernel.php`.
